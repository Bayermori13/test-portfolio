# پروژه تستی من
این یک پروژه تستی برای یادگیری گیت و گیت‌هاب است.

git config --global user.name "morteza neysi"
git config --global user.email "moribayer9@gmail.com"
git remote add origin https://github.com/Bayermori13/test-portfolio.git
git init
git add .
git commit -m "feat: initial commit"
git branch -M main
git remote add origin <لینک-ریپازیتوری-جدید>
git push -u origin main
### ابزارها:
- Git
- GitHub


git config --global user.name "morteza neysi"
git config --global user.email "moribayer9@gmail.com"
git remote add origin https://github.com/Bayermori13/test-portfolio.git
git init
git add .
git commit -m "feat: initial commit"
git branch -M main
git remote add origin <لینک-ریپازیتوری-جدید>
git push -u origin main